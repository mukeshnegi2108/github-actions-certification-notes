name: 02 - Workflow Events

on:
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        # This step prints the event name that triggered the workflow
        run: echo "Triggered by event ${{ github.event_name }}"
